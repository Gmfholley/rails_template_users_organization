<%= form_for(@organizations_user) do |f| %>
  <% if @organizations_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organizations_user.errors.count, "error") %> prohibited this organizations_user from being saved:</h2>

      <ul>
      <% @organizations_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
